apiVersion: v1
kind: Secret
metadata:
  name: kutt
  labels:
    {{- include "kutt.labels" . | nindent 4 }}
stringData:
    {{- range $key, $val := .Values.secretEnv }}
    {{ $key  }}: {{ b64enc $val }}
    {{- end }}