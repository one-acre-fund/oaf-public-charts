apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fineract-rest-pvc
spec:
  storageClassName: {{ .Values.storageClassName }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
