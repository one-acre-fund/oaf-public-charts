apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fineract-rest-pvc
spec:
  storageClassName: {{ .Values.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
