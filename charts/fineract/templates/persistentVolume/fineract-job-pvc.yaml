apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fineract-job-pvc
spec:
  storageClassName: {{ .Values.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi