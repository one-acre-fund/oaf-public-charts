apiVersion: v1
kind: Service
metadata:
  name: {{ template "fineract.report.fullname" . }}
  labels:
    app: fineract-report
    tier: middle
spec:
  type: ClusterIP
  ports:
  - port: 8443
  selector:
    app: fineract-report
    tier: middle
